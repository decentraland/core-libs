---
"@dcl/urn-resolver": patch
---

Migrate `@dcl/urn-resolver` from the standalone `urn-resolver` repository into the `core-libs` monorepo. The package source, tests and public API are unchanged — downstream consumers should see no behavioural differences.
